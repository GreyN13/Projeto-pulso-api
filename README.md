# Pulso API

Para funcionar no seu computador:

1. Copie `.env.example` para `.env`.
2. No `.env`, troque `MYSQL_ROOT_PASSWORD` pela sua senha local.
3. Execute `docker compose up --build` e abra `http://localhost:8080/swagger-ui/index.html`.

Login de teste: `gray.fox@pulso.com` / `Pulso@2026`.
