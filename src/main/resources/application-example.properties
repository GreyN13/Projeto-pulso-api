# Copie os valores para variáveis de ambiente ou para config/application-local.properties.
# Não versionar config/application-local.properties, pois ele pode conter uma senha real.
# Para carregar as contas de demonstração, execute com SPRING_PROFILES_ACTIVE=demo.
# O perfil demo usa o banco pulso_demo por padrão, separado do banco pulso.
spring.datasource.url=jdbc:mysql://localhost:3306/pulso
spring.datasource.username=SEU_USUARIO_MYSQL
spring.datasource.password=SUA_SENHA_MYSQL
